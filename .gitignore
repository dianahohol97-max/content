# Environment
.env
google-service-account.json

# Node
node_modules/
npm-debug.log

# OS
.DS_Store
Thumbs.db

# Keep output images/videos in repo (Make.com fetches via raw URL)
# but ignore the large text pack JSON (regenerated each run)
output/*.json
output/**/_manifest.json

# Intermediate render work dirs (never commit — only final MP4s)
output/**/_work_*/
**/_work_*/
*.srt
scenes.txt
audio.txt
